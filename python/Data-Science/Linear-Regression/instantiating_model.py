from sklearn.linear_model import LinearRegression

model = LinearRegression()
print(model)

# No scikit-learn, cada classe de modelo é representada por uma classe em python .
# Uma classe de modelo não é a mesma que uma instância de um modelo.
# Lembre-se de que a instância é um obje    to individual de uma determinada classe.